@using Microsoft.AspNetCore.Identity
@{
    ViewData["Title"] = "Access Denied";
    Layout = null;
}

<div class="text-center" style="margin-top: 50px;">
    <h2 class="text-danger">🚫 ليس لديك صلاحية للوصول إلى هذه الصفحة</h2>
    <p>يرجى تسجيل الخروج ثم تسجيل الدخول بحساب إداري إذا كنت تملك الصلاحية.</p>

    <form asp-action="Logout" asp-controller="Auth" method="post" style="margin-top: 20px;">
        <button type="submit" class="btn btn-danger">تسجيل الخروج</button>
    </form>
</div>
